[project]
name = "feishu-bridge"
version = "0.1.0"
description = "Bridge Feishu messages to Codex threads."
requires-python = ">=3.10"
dependencies = [
  "lark-oapi>=1.5.5",
  "requests>=2.31.0",
  "websocket-client>=1.9.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

